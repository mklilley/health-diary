// No text prompt is supplied: transcription preserves the original spoken material.
export const version = '1';
export const prompt = null;
export const transcriptionOptions = Object.freeze({ response_format: 'json' });
